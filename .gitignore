# Local helper scripts (Cowork commit flow) — never commit
_commit.ps1
_run_commit.bat
_run_*.bat
_make_zip.ps1
_make_zip.bat
_make_review.ps1
_make_review.bat
_make_audit*.ps1
_make_audit*.bat
og-template.html

# Claude instructions — local-only, never push to public repo
CLAUDE.md

# Internal handoff documents between Cowork sessions — never push
_handoff_*.md
TINYUSEFUL_REVIEW_BRIEFING.md

# Preview/mockup files — local-only experiments, never deploy to production
_preview-*.html

# Pre-patch backup snapshots — local-only checkpoints from iterative edits
# (e.g. style.css.pre-patchA-2026-05-13, index.html.pre-homepage-2026-05-13)
# Existing tracked backups are not affected by this rule; only untracked
# new backups will be ignored going forward.
*.pre-*

# Review ZIP archivy a kontrolni slozky — nikdy do live webu
tinyuseful-review-*.zip
kontrola GPT a Geminy/
kontrola pro GPT a gemny */

# Font ZIP downloads — nikdy do live webu (jen .woff2 v podslozkach)
fonts/*.zip

# Tokens & secrets — never commit (should be outside repo anyway)
tokeny.txt
.env
.env.local
*.key
*.pem

# OS / editor noise
Thumbs.db
.DS_Store
*.swp
*.swo
*~
