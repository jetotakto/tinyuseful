# Local helper scripts (Cowork commit flow) — never commit
_commit.ps1
_run_commit.bat
_run_*.bat
_make_zip.ps1
_make_zip.bat
_make_review.ps1
_make_review.bat
og-template.html

# Claude instructions — local-only, never push to public repo
CLAUDE.md

# Review ZIP archivy a kontrolni slozky — nikdy do live webu
tinyuseful-review-*.zip
kontrola GPT a Geminy/
kontrola pro GPT a gemny */

# Font ZIP downloads — nikdy do live webu (jen .woff2 v podslozkach)
fonts/*.zip

# Tokens & secrets — never commit (should be outside repo anyway)
tokeny.txt
.env
.env.local
*.key
*.pem

# OS / editor noise
Thumbs.db
.DS_Store
*.swp
*.swo
*~
